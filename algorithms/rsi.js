// const RSI = require('technicalindicators').RSI; https://www.npmjs.com/package/technicalindicators

// const rsi = (data, period = 14) => {
//     const prices = data.map(d => d.close);
//     const rsi = RSI.calculate({ values: prices, period: period });
//     return rsi;
// }